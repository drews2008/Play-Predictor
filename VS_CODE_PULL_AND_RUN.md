# VS Code: Pull latest and run dev

Use either of these in the VS Code terminal:

```bash
npm run pull:main
npm run dev
```

Or one command:

```bash
npm run dev:latest
```

## VS Code Task Runner

From **Terminal → Run Task**:
- `PlayPredictor: Pull latest main`
- `PlayPredictor: Pull latest + Run dev`
