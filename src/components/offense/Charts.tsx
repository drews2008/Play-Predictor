import React from "react";
import { PlayLogEntry } from "./TendencyDashboard";

// Placeholder component; replace with actual chart logic later
const Charts: React.FC<{ playLogs: PlayLogEntry[] }> = ({ playLogs }) => {
  return (
    <div>
      <h2>Charts</h2>
      <p>Chart rendering goes here.</p>
    </div>
  );
};

export default Charts;
